config BR2_PACKAGE_ELEMINES
	bool "elemines"
	select BR2_PACKAGE_EFL
	select BR2_PACKAGE_LIBETROPHY
	depends on BR2_INSTALL_LIBSTDCPP # efl
	depends on BR2_PACKAGE_HAS_UDEV # efl -> libudev
	depends on BR2_TOOLCHAIN_HAS_THREADS # efl
	depends on BR2_USE_MMU # efl
	depends on BR2_USE_WCHAR # efl
	help
	  Elemines is an EFL minesweeper clone.

	  http://elemines.sourceforge.net/

comment "elemines needs a toolchain w/ C++, threads, wchar"
	depends on !BR2_INSTALL_LIBSTDCPP || !BR2_TOOLCHAIN_HAS_THREADS || !BR2_USE_WCHAR
