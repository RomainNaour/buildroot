config BR2_PACKAGE_TERMINOLOGY
	bool "terminology"
	select BR2_PACKAGE_EFL
	select BR2_PACKAGE_LIBELEMENTARY
	depends on BR2_INSTALL_LIBSTDCPP # efl
	depends on BR2_PACKAGE_HAS_UDEV # efl -> libudev
	depends on !BR2_TOOLCHAIN_USES_UCLIBC # efl
	depends on BR2_USE_MMU # efl
	help
	  Terminology is an EFL terminal emulator with some extra bells
	  and whistles.

comment "terminology needs udev /dev management and a (e)glibc or musl toolchain w/ C++"
	depends on !BR2_PACKAGE_HAS_UDEV || !BR2_INSTALL_LIBSTDCPP || BR2_TOOLCHAIN_USES_UCLIBC
	depends on BR2_USE_MMU
